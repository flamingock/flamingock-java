This is discussion. Don´t change code.


Actually, I don´t fully agree with your approach.

you are suggesting to transform the writer to pass and JournalEvent<ChangeState>, just because the event contains the changeState inside.

However, I see it from the other perspective. The JournalEvent is horizontal, it may carry an ChangeState, as it can carry something else.

Thinking deeper, it´s actually a kind of "sidecar" object, in the sense that the target is the changeState(or in another context, the Execution or something else. The JournalEvent is horizontal to all that. so the writer shouldn´t take that event).
So my idea from the high perspective is to keep the keep the domains well defined, so we can have the ChangeStateOperation(Curently AuditStoreStepOperations), that receives a context , audit etc. It also has in its constructor an auditWriter.
So my suggestion is to also have an object that builds  the event. It´s your concept of JournalSequencer but taken to another level, it´s kind of stateful builder/factory that also cover the sequencer(it can also be delegated if you wish) and it´s
passed to the operations in the constructor.

So far so good,  I haven´t gone into too much detail. One of the challenges is that the operations is built for every change. The reasons is because we set in the constructor the TxType and the targetSystemId.
About this we have two options, retrieving both from outside and passing as we are passing the context or keep building the operations per chagne, but we holds the same Journal builder/factory per stage and inject it for every change.

I would probably lean into making the AuditOperations  per stage, passing the journal builder/factory at construction and the txType and the targetSystem in the methods, somehow.


Thoughts?
